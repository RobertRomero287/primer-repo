/*
Cree un programa que muestre los números naturales de 1 a n. Use ciclo while.
*/

let num = parseInt(prompt(`ingrese el numero N : `))
let index = 1;

while(index <= num){
    console.log(index);
    index++;
}