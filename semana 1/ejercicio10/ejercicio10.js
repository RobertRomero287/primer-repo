let x = -5
let valorAbsoluto;
if (x >= 0) {valorAbsoluto = x;}
else {valorAbsoluto = -x;}
console.log ("EL valor absoluto es=",valorAbsoluto);

// practica 2 
let y = 9
let valortotal;
if (y >= 0) {valortotal = y;}
else {valortotal = -y;}
console.log ("el valor total es=", valortotal);

//if (x >= 0) {
 //valorAbsoluto = x; // Si x es positivo o cero, el valor absoluto es x} 
 //else {valorAbsoluto = -x; // Si x es negativo, el valor absoluto es -x}
//Cree un programa que tome un número real e imprima su valor absoluto. 
// si  x es mayor o igual a 0, el valor absoluto es  x. Si  x es menor que 0, el valor absoluto es  −x.